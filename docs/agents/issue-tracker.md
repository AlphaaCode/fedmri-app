# Issue tracker — local markdown

Issues live as `.md` files under `.scratch/{slug}/issue.md`.

Create: `mkdir -p .scratch/{slug} && cat > .scratch/{slug}/issue.md`
List: `ls .scratch/`
Close: add `**Closed**: {date}`, move to `.scratch/done/{slug}/`
