# Domain docs — single-context

- `CONTEXT.md` at repo root — vocabulary, model arch, performance numbers, clinical facts
- `docs/adr/` — architectural decisions (read before structural changes)
- `apps/backend/prisma/schema.prisma` — source of truth for data shape
- `packages/shared/src/types/index.ts` — shared TypeScript types

Skills: `improve-codebase-architecture`, `diagnose`, `tdd`, `zoom-out` all read CONTEXT.md first.
